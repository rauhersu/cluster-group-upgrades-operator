apiVersion: kuttl.dev/v1beta1
kind: TestStep

commands:
  # Start precaching
  - command: oc --namespace=default patch clustergroupupgrade.ran.openshift.io/cgu --patch '{"spec":{"enable":true,"preCaching":true}}' --type=merge
    ignoreFailure: false
